localhost {
    # Matches and strips the /api prefix, then proxies to backend
    handle_path /api/* {
        reverse_proxy backend:3000
    }

    # All other requests go to the frontend
    reverse_proxy frontend:5173
}
